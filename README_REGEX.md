## Simple readme for save regex tips

* . = any character
* * = extends for one or any characters, like (.*) = all characters
* ? = delimiter = (.*?)teste
* \d\D = any digit ou nom digit
* ^ = negative delimiter
